# Aim is to collect data about the elevator system the user wish to convert to.
# Please mention the system (american or european) do you wish to convert to:
var = input("Mention the system's first letter of which do you want to convert to; for american enter 'a' and for european enter 'e': ")

# To validate the user's input as acceptable or inacceptable.
if var == 'a' or var== 'e': 
    print ("VALID INPUT")
else:
    print("INVALID INPUT. Please enter the letter 'a' or 'e'")

# To convert the floor number from one system to another depending on user's requirement.    
if var == 'a':
    flr = input("Enter your european floor number: ")
    aflr = int(flr) + 1
    print("Your American floor number is:", aflr)
elif var == 'e':
    flr = input("Enter your american floor number: ")
    eflr = int(flr) - 1
    print("Your european floor number is:", eflr)

